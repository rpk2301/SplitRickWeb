version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands: []
        build:
          commands: []   # static site, nothing to build
      artifacts:
        baseDirectory: public
        files:
          - '**/*'
      cache:
        paths: []